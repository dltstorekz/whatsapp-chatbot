def handle(text: str) -> str:
    """
    Example handler that reverses the input text and returns the reversed string.
    :param text: input string from user
    :return: reversed string
    """
    # Reverse the string and return
    return text[::-1]
